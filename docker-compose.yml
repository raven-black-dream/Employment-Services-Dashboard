version: '3.7'

networks:
    frontend:
        external: true

services:
    app:
        build: ./
        command: streamlit run app/main.py --server.port 8501
        volumes:
            - ./:/app
        ports:
            - 8501:8501
        image: employment-services-dashboard:latest
        container_name: employment-services-dashboard
        networks:
            - frontend
